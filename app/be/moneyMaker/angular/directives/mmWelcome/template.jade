div
    nav.navbar.navbar-default(role='navigation')
        .container-fluid
            .navbar-header
                button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#bs-example-navbar-collapse-1')
                    span.sr-only Toggle navigation
                    span.icon-bar
                    span.icon-bar
                    span.icon-bar
                a.navbar-brand(href='#') Money Maker
            #bs-example-navbar-collapse-1.collapse.navbar-collapse
                ul.nav.navbar-nav
                    li(ng-class="{'active' : $root.isMenuCurrentlySelected('/currency') == true}")
                        a(ng-click='nav(\'/currency\')') Currency
                    li(ng-class="{'active' : $root.isMenuCurrentlySelected('/stock') == true}")
                        a(ng-click='nav(\'/stock\')') Stock
                    li(ng-class="{'active' : $root.isMenuCurrentlySelected('/myShare') == true}")
                        a(ng-click='nav(\'/myShare\')') My share

    div(ng-view)